# tavily-python 收紧到 0.7.x：SDK 行为已验证（0.8+ 可能有破坏性变更，
# 历史踩坑：research 无 timeout、强制 stream、/usage 字段差异等）
tavily-python~=0.7.0
mcp>=1.0.0,<2.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
# pywebview 需 6.x：create_window 的 x/y 参数与 private_mode 迁移到
# webview.start() 是 6.x 行为（本项目已依赖）
pywebview>=6.0
cryptography>=42.0.0
